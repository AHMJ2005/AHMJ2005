#include <stdio.h>

int	*ft_range(int min, int max);

int	main(void)
{
	int	*tab;
	int	i;
	int	min = 3;
	int	max = 8;

	tab = ft_range(min, max);
	/*if (!tab)
		return (0);*/
	i = 0;
	while (i < max - min)
	{
		printf("%d\n", tab[i]);
		i++;
	}
}
